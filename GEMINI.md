# GEMINI.md (adapter)

Follow **[AGENTS.md](./AGENTS.md)** as the project rules file.

This stub exists for Gemini CLI context discovery. Prefer configuring
`.gemini/settings.json` with `"context": { "fileName": "AGENTS.md" }` so
`AGENTS.md` is the single always-on source. Do not fork instructions here.

Project skills: `.agents/skills/` (canonical; Gemini also discovers
`.agents/skills` and `.gemini/skills` after sync).
