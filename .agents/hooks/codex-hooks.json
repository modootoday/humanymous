{
  "description": "Project safety guard for Codex Auto mode.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/python3 \"$(git rev-parse --show-toplevel)/scripts/agents/hooks/pre-tool-guard.py\"",
            "commandWindows": "powershell -NoProfile -Command \"$root = git rev-parse --show-toplevel; py -3 (Join-Path $root 'scripts/agents/hooks/pre-tool-guard.py')\"",
            "timeout": 10,
            "statusMessage": "Checking workspace safety policy"
          }
        ]
      }
    ]
  }
}
