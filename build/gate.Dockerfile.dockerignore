# Per-Dockerfile ignore (overrides the root .dockerignore) so the Gate build
# can see cmd/gate + internal/. Keep only heavy/irrelevant paths out.
.git
.github
bin
*.exe
web/detector.wasm
docs
sots
plan
node_modules
test/node_modules
**/*.log
